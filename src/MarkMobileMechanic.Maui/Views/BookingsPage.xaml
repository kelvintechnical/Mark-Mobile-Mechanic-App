<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MarkMobileMechanic.Maui.Views.BookingsPage"
             Title="Bookings">
    <CollectionView ItemsSource="{Binding Bookings}" Margin="16">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="0,8" Padding="12">
                    <VerticalStackLayout>
                        <Label Text="{Binding Status}" FontAttributes="Bold" />
                        <Label Text="{Binding StartTime, StringFormat='Start: {0:G}'}" />
                        <Label Text="{Binding EndTime, StringFormat='End: {0:G}'}" />
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
