# Entity Framework Core Migrations

This folder contains placeholder migration files for the initial schema. Regenerate migrations with:

```
dotnet ef migrations add InitialCreate \
  --project src/MarkMobileMechanic.Infrastructure \
  --startup-project src/MarkMobileMechanic.Api
```
